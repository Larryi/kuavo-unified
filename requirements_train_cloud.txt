# Minimal cloud-training environment. The base image should already provide a
# CUDA-compatible PyTorch; uv will keep it when it satisfies LeRobot's bounds.
-e third_party/lerobot
-e .

accelerate==1.11.0
tensorboard==2.20.0
hydra-core==1.3.2
timm==1.0.19
kornia==0.8.1
scipy
matplotlib
rich==14.1.0
